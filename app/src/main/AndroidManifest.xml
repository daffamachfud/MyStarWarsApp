<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.onoh.mystarwarsapp">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ui.detail.detailspesies.DetailSpeciesActivity" />
        <activity android:name=".ui.detail.detailstarship.DetailStarshipActivity" />
        <activity android:name=".ui.detail.detailfilm.DetailFilmActivity" />
        <activity android:name=".ui.detail.detailpeople.DetailProfileActivity" />
        <activity android:name=".ui.home.news.NewsWebActivity" />
        <activity android:name=".ui.planet.PlanetActivity" />
        <activity android:name=".ui.species.SpeciesActivity" />
        <activity android:name=".ui.starship.StarshipActivity" />
        <activity android:name=".ui.vehicles.VehiclesActivity" />
        <activity android:name=".ui.film.FilmActivity" />
        <activity android:name=".ui.people.PeopleActivity" />
        <activity
            android:name=".ui.MainActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>